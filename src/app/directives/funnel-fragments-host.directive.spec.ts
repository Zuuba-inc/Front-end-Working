import { FunnelFragmentsHostDirective } from './funnel-fragments-host.directive';

describe('FunnelFragmentsHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FunnelFragmentsHostDirective();
    expect(directive).toBeTruthy();
  });
});
