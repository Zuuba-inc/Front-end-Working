import { ComponentBuildersHostDirective } from './component-builders-host.directive';

describe('ComponentBuildersHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentBuildersHostDirective();
    expect(directive).toBeTruthy();
  });
});
